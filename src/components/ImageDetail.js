import React from 'react';
import {Text, StyleSheet, View} from 'react-native';

const ImageDetail = (props) => {
    return <Text>Show image of {props.title}</Text>;
};

const styles = StyleSheet.create({});

export default ImageDetail;
